// import { extendTheme } from "@chakra-ui/react";

// const theme = extendTheme({
//   config: {
//     initialColorMode: "light", // Ensure it's set to light
//     useSystemColorMode: false, // Prevent system override
//   },
// });

// export default theme;